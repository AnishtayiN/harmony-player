name: Build & Release Harmony Player

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

env:
  FLUTTER_VERSION: '3.24.0'
  JAVA_VERSION: '17'
  APP_NAME: harmony-player

permissions:
  contents: write

jobs:
  # ═══════════════════════════════════════════════════════════
  # ANDROID BUILD
  # ═══════════════════════════════════════════════════════════
  build-android:
    name: 🤖 Android APK
    runs-on: ubuntu-latest
    steps:
      # ⭐ Upgrade to v5 (Node 24 compatible)
      - uses: actions/checkout@v5

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: ${{ env.JAVA_VERSION }}

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: 'stable'
          cache: true

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Accept Android Licenses
        run: yes | sdkmanager --licenses || true

      # ⭐ Create assets folder if not exists
      - name: Create assets directory
        run: |
          mkdir -p assets/images
          touch assets/images/.gitkeep

      - name: Install dependencies
        run: flutter pub get

      - name: Generate code
        run: dart run build_runner build --delete-conflicting-outputs

      - name: Decode Keystore
        if: ${{ env.ANDROID_KEYSTORE_BASE64 != '' }}
        env:
          ANDROID_KEYSTORE_BASE64: ${{ secrets.ANDROID_KEYSTORE_BASE64 }}
        run: |
          echo "$ANDROID_KEYSTORE_BASE64" | base64 --decode > android/app/keystore.jks

      - name: Create key.properties
        if: ${{ env.ANDROID_KEYSTORE_BASE64 != '' }}
        env:
          ANDROID_KEYSTORE_BASE64: ${{ secrets.ANDROID_KEYSTORE_BASE64 }}
        run: |
          echo "storePassword=${{ secrets.ANDROID_KEYSTORE_PASSWORD }}" > android/key.properties
          echo "keyPassword=${{ secrets.ANDROID_KEY_PASSWORD }}" >> android/key.properties
          echo "keyAlias=${{ secrets.ANDROID_KEY_ALIAS }}" >> android/key.properties
          echo "storeFile=keystore.jks" >> android/key.properties

      # ⭐ Add set -e for fail-fast
      - name: Build APK (split per ABI)
        run: |
          set -e
          flutter build apk --release --split-per-abi

      - name: Build APK (universal)
        run: |
          set -e
          flutter build apk --release

      - name: Rename artifacts
        run: |
          set -e
          cd build/app/outputs/flutter_apk
          mv app-release.apk ${{ env.APP_NAME }}-${{ github.ref_name }}-universal.apk
          [ -f app-arm64-v8a-release.apk ] && mv app-arm64-v8a-release.apk ${{ env.APP_NAME }}-${{ github.ref_name }}-arm64.apk || true
          [ -f app-armeabi-v7a-release.apk ] && mv app-armeabi-v7a-release.apk ${{ env.APP_NAME }}-${{ github.ref_name }}-arm.apk || true
          [ -f app-x86_64-release.apk ] && mv app-x86_64-release.apk ${{ env.APP_NAME }}-${{ github.ref_name }}-x86_64.apk || true

      - uses: actions/upload-artifact@v4
        with:
          name: android-artifacts
          path: build/app/outputs/flutter_apk/*.apk

  # ═══════════════════════════════════════════════════════════
  # iOS BUILD
  # ═══════════════════════════════════════════════════════════
  build-ios:
    name: 📱 iOS IPA
    runs-on: macos-15  # ⭐ Use macos-15 instead of macos-latest to avoid macOS 26 issues
    steps:
      - uses: actions/checkout@v5

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: 'stable'
          cache: true

      - name: Install CocoaPods
        run: sudo gem install cocoapods

      # ⭐ Create assets folder
      - name: Create assets directory
        run: |
          mkdir -p assets/images
          touch assets/images/.gitkeep

      - name: Install dependencies
        run: flutter pub get

      - name: Generate code
        run: dart run build_runner build --delete-conflicting-outputs

      - name: Install iOS pods
        run: |
          set -e
          cd ios
          pod install --repo-update

      - name: Build iOS (no-codesign)
        run: |
          set -e
          flutter build ios --release --no-codesign

      - name: Create IPA archive
        run: |
          set -e
          mkdir -p Payload
          cp -r build/ios/iphoneos/Runner.app Payload/
          zip -r ${{ env.APP_NAME }}-${{ github.ref_name }}-ios.ipa Payload

      - uses: actions/upload-artifact@v4
        with:
          name: ios-artifacts
          path: ${{ env.APP_NAME }}-${{ github.ref_name }}-ios.ipa

  # ═══════════════════════════════════════════════════════════
  # WINDOWS BUILD (Windows 7/8/10/11)
  # ═══════════════════════════════════════════════════════════
  build-windows:
    name: 🪟 Windows (7/8/10/11)
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v5

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: 'stable'
          cache: true

      # ⭐ Create assets folder
      - name: Create assets directory
        run: |
          New-Item -ItemType Directory -Force -Path "assets\images"
          New-Item -ItemType File -Force -Path "assets\images\.gitkeep"
        shell: pwsh

      - name: Install dependencies
        run: flutter pub get

      - name: Generate code
        run: dart run build_runner build --delete-conflicting-outputs

      - name: Build Windows
        run: |
          $ErrorActionPreference = 'Stop'
          flutter build windows --release
        shell: pwsh

      - name: Create Portable ZIP
        run: |
          $ErrorActionPreference = 'Stop'
          Compress-Archive -Path "build\windows\x64\runner\Release\*" -DestinationPath "${{ env.APP_NAME }}-${{ github.ref_name }}-windows-x64.zip" -Force
        shell: pwsh

      - uses: actions/upload-artifact@v4
        with:
          name: windows-artifacts
          path: ${{ env.APP_NAME }}-${{ github.ref_name }}-windows-x64.zip

  # ═══════════════════════════════════════════════════════════
  # macOS BUILD
  # ═══════════════════════════════════════════════════════════
  build-macos:
    name: 🍎 macOS
    runs-on: macos-15  # ⭐ Use macos-15 to avoid macOS 26 breaking changes
    steps:
      - uses: actions/checkout@v5

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: 'stable'
          cache: true

      # ⭐ Create assets folder
      - name: Create assets directory
        run: |
          mkdir -p assets/images
          touch assets/images/.gitkeep

      - name: Install dependencies
        run: flutter pub get

      - name: Generate code
        run: dart run build_runner build --delete-conflicting-outputs

      - name: Install CocoaPods
        run: sudo gem install cocoapods

      - name: Install macOS pods
        run: |
          set -e
          cd macos
          pod install --repo-update

      - name: Build macOS
        run: |
          set -e
          flutter build macos --release

      - name: Create DMG
        run: |
          set -e
          brew install create-dmg
          create-dmg \
            --volname "Harmony Player" \
            --window-pos 200 120 \
            --window-size 800 400 \
            --icon-size 100 \
            --app-drop-link 600 185 \
            "${{ env.APP_NAME }}-${{ github.ref_name }}-macos.dmg" \
            "build/macos/Build/Products/Release/"

      - uses: actions/upload-artifact@v4
        with:
          name: macos-artifacts
          path: ${{ env.APP_NAME }}-${{ github.ref_name }}-macos.dmg

  # ═══════════════════════════════════════════════════════════
  # LINUX BUILD
  # ═══════════════════════════════════════════════════════════
  build-linux:
    name: 🐧 Linux
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Install Linux dependencies
        run: |
          set -e
          sudo apt-get update
          sudo apt-get install -y clang cmake ninja-build pkg-config \
            libgtk-3-dev liblzma-dev libstdc++-12-dev \
            libgstreamer1.0-dev libgstreamer-plugins-base1.0-dev \
            libgstreamer-plugins-bad1.0-dev

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: 'stable'
          cache: true

      # ⭐ Create assets folder
      - name: Create assets directory
        run: |
          mkdir -p assets/images
          touch assets/images/.gitkeep

      - name: Install dependencies
        run: flutter pub get

      - name: Generate code
        run: dart run build_runner build --delete-conflicting-outputs

      - name: Build Linux
        run: |
          set -e
          flutter build linux --release

      - name: Create tar.gz
        run: |
          set -e
          cd build/linux/x64/release/bundle
          tar -czvf ${{ env.APP_NAME }}-${{ github.ref_name }}-linux.tar.gz *

      - uses: actions/upload-artifact@v4
        with:
          name: linux-artifacts
          path: build/linux/x64/release/bundle/${{ env.APP_NAME }}-${{ github.ref_name }}-linux.tar.gz

  # ═══════════════════════════════════════════════════════════
  # CREATE GITHUB RELEASE
  # ═══════════════════════════════════════════════════════════
  release:
    name: 🚀 Publish Release
    needs: [build-android, build-ios, build-windows, build-macos, build-linux]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts
          merge-multiple: false

      - name: Collect release files
        run: |
          set -e
          mkdir -p release
          find artifacts -type f \( -name "*.apk" -o -name "*.ipa" -o -name "*.dmg" -o -name "*.tar.gz" -o -name "*.zip" \) -exec cp {} release/ \;
          ls -la release/

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: release/*
          generate_release_notes: true
          draft: false
          prerelease: false
          name: Harmony Player ${{ github.ref_name }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
